# 2026-09-01 工作日志

## push 补推成功（main + v0.12.0 tag 全部上远端）
- 本机代理 **5016 / 7897 均不可用**（5016 返回 CONNECT 502，7897 走 CONNECT 后 TLS 握手失败：schannel/openssl 都报 unexpected eof）。**直连反而通**（curl 直连 api.github.com 200）。
- 正确姿势：`env -u NODE_OPTIONS -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u all_proxy git push ...`（摘掉 NODE_OPTIONS shim + 摘掉全部代理变量）。
- `--follow-tags` 未把 v0.12.0 带上，需显式 `git push origin refs/tags/v0.12.0`；网络抖动大，重试 3 次才成功（单次失败率 ~50%，21s 超时）。
- 注意：git 内置 push 重试循环写法要用 `out=$(...)` 存退出码，直接 `cmd | tail` 时 `$?` 是 tail 的退出码，循环会失效。

## Minke（lencx）竞品/同类项目分析
- 同类同底座（DeepSeek Harness）桌面工作台，v0.3.0，355 个 ts/tsx 共 39,320 行，77 个 test 文件 843 个 test()。浅克隆到 `%TEMP%/orchdesk-minke/repo`，三路并行深度审阅（dsh 集成与权限 / 插件·IM·浏览器·终端 / 数据层·发布链·测试工程）。
- **最该抄的三件事**：① `registerHooks`+esbuild 让 `node --test` 直跑 TS/TSX/CSS（消灭我们「UMD-lite 双环境单文件」hack 与「改源码必重编译」反馈环）；② 架构守护测试（module-boundaries 扫 import 图 + 内嵌 knownViolation 自检防规则失效）——我们的铁律目前只靠自觉；③ 高危路径工程纪律：命令白名单 parser + exactKeys、operationId 幂等 + uncertain 显式状态、迁移两阶段 journal + staging 硬链接 rename + 冲突保留、更新校验链（immutable/exact size/SHA256/URL 链/防降级/TOCTOU 二次 stat）。
- **产品能力差距**：无浏览器工具（它有 CDP 直连 + ref 机制 + 一次性非持久 partition + 控制权切换）、无 PTY 终端、无文件 Tab（Shiki 高亮 + CodeMirror merge diff）。我们已装 playwright 却未产品化；建议走 Electron 自带 CDP（零额外依赖、无用户态残留），Playwright 留给外部自动化。
- **明确不做**：runtime stage/prune/patch 流水线（我们有更轻的 vendor-dsh.cjs）、微信逆向协议、Tailscale/Cloudflare 远程访问、Electron-as-Node 跑 harness、其源码文本断言内容（只抄「棘轮基线」机制）。
- 提醒：Minke 43% 代码投在 IM/远程（我们的非优先项），借鉴工程纪律时别顺手搬它的产品重心——我们的差异化是多 Agent 编排 / 脑手解耦 / 意图网关。
- 产物：`%TEMP%/orchdesk-minke/Minke借鉴分析与OrchDesk吸收优化计划.md`（能力对照表 + S/A/B 分级借鉴清单 + P0–P3 分阶段计划 + 不做清单 + 风险）。

## P0 落地：TS 直测 loader + 架构守护测试（commit a17d9cd，ADR-0010，verify 674→702 / 20 套件）
- **零依赖 TS 直测 loader**（`apps/desktop/scripts/ts-loader-hooks.mjs` + `ts-load.cjs`）：基于 **Node 22.13+ 内置 `module.stripTypeScriptTypes()`（amaro/swc）+ ESM `module.register()` hooks**，没装 esbuild/ts-node（零安装失败风险、零供应链面）。
  - 关键实现点：① CJS loader **不认 .ts 扩展名**（`require('/x.ts')` 直接 MODULE_NOT_FOUND，加 `--experimental-strip-types` 也没用），必须走 ESM `import()` + hooks；② hooks 里 `load` 用 `new URL(url).pathname` 判断后缀（而非 `url.endsWith('.ts')`），才能支持 `?t=N` cache-busting；③ resolve 要先剥 `?query#hash` 再探磁盘、命中后原样拼回；④ 只接管 `.ts`，`.js/.json/.node` 交回 `nextResolve/nextLoad` —— 因此 **stub electron 的 `Module._load` 钩子 + require('dist/main.js') 完全不受影响**（有回归用例守护）。
  - 我们的 TS 源码正好满足 strip 约束：无 enum / namespace / 参数属性 / 装饰器，纯逻辑模块只 import node 内置 + 无扩展名相对路径。
- **一致性硬约束**：`ts-loader-verify.cjs` B 组断言「同一输入 TS 版与 dist 版输出逐 JSON 相等」，否则 loader 引入语义漂移 → 直测结果不可信。C 组证明收益：覆写 fixture 源码后不跑 tsc，断言立刻读到新值。
- **架构守护测试**（`arch-guard-verify.cjs`，15 项）：R1 渲染层禁 Node/Electron / R2 纯逻辑零 electron / R3 纯逻辑不依赖宿主层 / R4 渲染层不持久化不执行工具 / R5 禁硬编码本机路径与密钥 / R6 插件产物链路不陈旧（src→lib→vendor）/ R7 套件必须在 verify 链上。
  - **元规则自检（Minke knownViolation 思路，最关键）**：M1 每条规则必须命中自己的正样本（抓不到 = 规则失效 FAIL）、M2 扫描面非空（glob 写错 = 规则空转 FAIL）、M3 白名单文件须存在、M4 白名单确实零 electron。理由：架构规则是**否定式断言**，失效时静默通过——比没规则更危险（虚假安全感）。
  - 扫描前剥离块注释与整行 `//`（不剥行尾，免误伤 `http://`），否则 session-fork.js 注释里的 require 示例会误报。
- **R6 当场抓到真陈旧**：compensation / evolution / multi / prompt 四个插件 `src` 比 `lib` 新（正是记忆里「改插件源码忘了 tsc+vendor 跑出假结果」那个坑）——已重编译 + vendor 修复。**这就是机器守护的价值：人肉纪律会失效。**
- 负向验证做全：注入违规 renderer 文件 → R1/R4 正确 FAIL；touch plugin src → R6 正确 FAIL；touch .ts → 陈旧探针正确 FAIL。不是写完就信。
- ADR-0010 已落 `docs/70-决策/`（含背景/决策/理由/后果：Node ≥22.13 成为验证链路硬要求，打包运行不受影响；loader 只用于测试不进打包产物）。

## v0.12.0 GitHub Release 发布（第一个真正发出的 Release）
- **CI 流程真相**：`v*` tag 推送后，workflow 自动打包并 **以 Draft 形式上传资产**（latest.yml + Setup + portable），但**不会自动发布**。必须人工 `gh release edit <tag> --notes-file … --draft=false` 转正。此前 Release 一直停在 v0.4.1 就是这个原因——tag 打了、CI 跑了，但没人点发布。
- **untagged URL 陷阱**：tag 推送前若已有同名 Draft，其 URL 会显示 `untagged-<hash>`；转正后自动变回 `/releases/tag/v0.12.0`（tagName 字段一直是对的，不用担心）。
- **gh 必须在 git 仓库内执行**（`--notes-file` 传绝对路径也不行，报 `not a git repository`）。
- **网络**：宿主侧 `api.github.com` 通、`github.com` **下载域不通**（curl 000 / exit 35，gh download 走 GraphQL 报 EOF）。验证资产改用 `gh release view --json assets` 看 `state:uploaded` + size，不要靠下载验证。
- **blockmap 未上传但无害**：`latest.yml` 不引用 blockmap（无 blockmapSize/Url 字段），缺失只影响差分更新、不影响自动更新生效。
- 发布结果：https://github.com/ra1nzzz/orchdesk/releases/tag/v0.12.0（3 资产，Setup 85.3MB / portable 84.9MB / latest.yml）。
- 注意 CI 产物与本地产物 size 不同（85289850 vs 85272247），源于构建环境与签名时间戳；二者代码同为 fd2dc9f。CI 产物未做实机冒烟（宿主无 GUI + 下载域不通），需用户实机确认。

## P1 落地：浏览器工具（ADR-0011，Electron 自带 CDP，8 个工具，verify 702→741 / 21 套件）
- **选 CDP 不选 `executeJavaScript`**：前者支持 `awaitPromise`/`returnByValue`/`userGesture`/超时，不受页面 CSP 与上下文污染影响，截图与导航等待同属一套协议。零新依赖（playwright 留给外部 E2E）。
- **共享用户默认 session 保留登录态**（能开电商后台/内部系统才是真能力），代价由**授权门**覆盖而非隔离回避：导航=边界外访问（域名白名单 + 补偿层外发二次确认，同 `web_fetch`）；`browser_click`/`browser_type`/`browser_eval` 走授权门（同 `file_write`，paranoid 全锁）；全部进沙箱日志 `kind:'browser'`；**拒绝时命令绝不下发**（有回归断言）。
- **分层**：`browser-tools.ts`（纯逻辑零 electron：schema/参数归一化/**页面内 JS 表达式构造**/脚本风险扫描/截图路径）+ `browser-cdp.ts`（宿主：窗口生命周期/attach/`Page.navigate`+加载等待/`Runtime.evaluate`/`Page.captureScreenshot` 落盘）+ `main.ts` 只接线。
- **唯一注入面是表达式构造**：模型给的选择器/文本一律 `JSON.stringify` 后嵌入，并在**假 DOM 里真跑一遍**断言引号/反斜杠/换行不逃逸；`buildTypeExpression` 用原型 setter + input/change 事件兼容 React/Vue 受控组件。
- **三个真 bug 由新套件当场抓出**：① `clampInt` 对 `''`/`undefined` 走 `Number('')===0` 被下限钳成最小值 →「模型没传 timeout」变成「给 500ms 超时，几乎必超时」（缺省必须直接回落 fallback）；② `getBrowserState()` 未打开时不返回 `lastShot` → 状态对象形状不稳定，渲染层得写兜底；③ 三处硬编码「7 个工具」的断言 → 改为 `TOOL_DEFS.length` 动态取数（写死常量会让「新增工具没下发」伪装成「套件过时」）。
- **`waitForLoad` 必须双保险**：以 `Page.loadEventFired`/`domContentEventFired` 为主 + 250ms 轮询 `document.readyState` 兜底（页面在 attach 前已加载完时事件不会再发一次）。debugger 的所有 CDP 事件都走同一个 `'message'`，method 在第二个参数里。
- **UI 是验收底线不是装饰**（写进 ADR）：Agent 在 `show:false` 后台窗口操作网页，用户没有观察/制动入口就是黑箱。标题栏「浏览器」入口 + 状态面板（标题/地址/最近截图缩略图/显示·隐藏·关闭/截图目录）+ `onBrowserState` 推送实时跟随；桥不可用时 `getBrowserStatus` 返回 `{open:false}` 表示「未接入」，不冒充「已就绪只是没开」。

## 真机 Electron 在此环境跑不起来的两个成因（别再重复排查）
1. **宿主向下继承 `ELECTRON_RUN_AS_NODE=1`** → `electron.exe` 退化成纯 Node，`require('electron')` 返回路径字符串、`app` 为 undefined（报错 `Cannot read properties of undefined (reading 'whenReady')`）。所有 electron 调用一律加 `env -u ELECTRON_RUN_AS_NODE`。
2. **即便摘掉它，非交互会话里 GPU 进程起不来**：`gpu_process_host.cc:956 GPU process exited unexpectedly: exit_code=1` 连续若干次 → `gpu_data_manager_impl_private.cc:416 FATAL: GPU process isn't usable. Goodbye.` → 进程被硬杀（退出码 127，stdout 都来不及刷）。`--disable-gpu`、`no-sandbox`、`in-process-gpu`、`disable-gpu-compositing` **全部无效**；非沙箱执行则直接被 SIGTERM。
- **结论与产物**：浏览器工具的真机 CDP 行为无法在本会话验证（CODE 层面只有「命令发出去了」的 stub 证明）。故新设 `apps/desktop/scripts/browser-smoke.cjs`（`pnpm run smoke:browser`，10 步：本地 http 测试页导航/取正文/取链接/填值回读引号反斜杠/点击命中/点击缺失元素必报错/截图落盘/状态 lastShot/关闭归零），**刻意不进 verify 链**（需要真 GPU，CI 与非交互会话必假红，反而诱使人放宽断言）。脚本启动期自检 `process.type`，非 Electron 主进程直接退出码 2 并打印上面两条指引。
- Electron 冒烟/驱动脚本**必须放在 `apps/desktop` 下**，否则 `require('electron')` 解析不到 node_modules（放 `%TEMP%` 会 MODULE_NOT_FOUND）。

## 用户开放完全访问后：真机冒烟跑通 11/11（上一节「跑不起来」的结论已被推翻，保留作为排查史）
- **真正能跑的组合**：`env -u ELECTRON_RUN_AS_NODE -u NODE_OPTIONS ORCHDESK_SMOKE_CI=1 <electron.exe> scripts/browser-smoke.cjs`。CI 模式做三件事：`ORCHDESK_BROWSER_NO_SANDBOX=1`（关 Chromium sandbox）+ `app.disableHardwareAcceleration()` + `disable-gpu`/`no-sandbox` 命令行开关。**`webPreferences.sandbox:true` 是致命项**——本会话里它让 GPU/渲染进程起不来，进程退出码 127 且 stdout 全空。
- **系统排查顺序（以后直接照抄）**：`--version` → `whenReady` 探针 → 建窗口 → attach → 逐条 CDP 命令。每一步都打**实时日志**到 stdout（不实时打印的话，进程一被杀就什么都留下，只能瞎猜）。
- **`timeout N` 包装会掩盖真相**：进程被外部硬杀是 127，被 timeout 杀是 124；而「静默退出 rc=0」往往是 **Electron 默认 `window-all-closed` 就 quit**（冒烟进程没有主窗口，关掉最后一个窗口即退出）→ 冒烟脚本必须 `app.on('window-all-closed', e => e.preventDefault())`。
- **冒烟过程中不要中途开关窗口**：关掉一个窗口后再新建窗口并 attach，会让主进程直接消失（127、无日志）。
- 自检先行：脚本第一步绕开产品代码手动开窗口加载页面，用来区分「环境不行」还是「代码不行」——这一步省了我一半时间。

## 真机暴露的三个挂起类问题（都不是「CI 怪事」，锁屏/远程桌面/最小化同样会发生）
1. **`Emulation.setDeviceMetricsOverride` 会让主进程直接消失**（退出码 127、stdout 空、连 try/catch 都来不及）。它只用于对齐视口与设备像素比（窗口本身 1280×900），价值有限 → 带 2s 超时 + 失败忽略，`NO_SANDBOX` 时直接跳过。**改完这一条，CDP 全链路就通了**（此前所有 CDP 命令挂起的假象都源于它）。
2. **`Page.captureScreenshot` 取不到合成帧时永久挂起，而不是报错**（实测挂 30s+）；同环境 Electron 原生 `webContents.capturePage()` **68ms 出图**。→ 截图 = CDP（超时默认 8s）+ capturePage 回退；缩略图改为拿已得 PNG 本地 `nativeImage.resize` 缩放，**不再发第二次截图请求**（CDP 挂起时第二次同样挂，连主结果都拿不到）。
3. **`Page.navigate` 挂起时页面根本打不开** → 回退 `win.loadURL()`（did-finish-load 才 resolve）；`Page.enable`/`Runtime.enable` 改为带超时的可降级调用（前者只为收 `loadEventFired`，后者对 `Runtime.evaluate` 非必需）；`Runtime.evaluate` 外层再加超时（`timeout` 参数只管页面内执行时长，管不了命令挂起），超时兜底 `executeJavaScript`。
- **原则**：CDP 出问题宁可降级也不要挂起——「工具没反应」比「降级后功能少一点」糟糕得多；且降级必须**可见**（结果带 `via`、沙箱日志写明「已回退 capturePage」、模型看到「视口截图」而非假装整页），不许用「跑通了」掩盖「其实是兜底跑通的」。
- 真机结果：导航/取正文/取链接/填值回读（引号反斜杠不逃逸）/点击命中/缺失元素报错/截图落盘 12408B（`via=cdp`）/状态 lastShot/关闭归零 **11/11**。
- 新回归 4 项（CDP 挂起→回退出图 / CDP 报错→回退 / 两条路都不通→如实报错 / shot 超时钳制），browser-tools 39→43，全量 verify **741→745**（21 套件，EXIT=0）。
- stub 侧同步增强：`DebuggerStub.screenshotMode`（ok/hang/reject）+ `webContents.capturePage` + `nativeImage.createFromBuffer`，否则回退路径只能靠注释存在。
