# 2026-08-19 OrchDesk 工作日志

## P2 继续（T-P2-4 / T-P2-5 真实逻辑实现）
- **T-P2-4 脑手解耦（orchdesk-brain）**：由骨架升级为真实 SubAgent 生命周期。dsh 原生机制：`ctx.agents.create(opts)` + `AgentHandle.dispose()`，opts.meta `{ origin:'subagent', delegationDepth:1, parentSession? }` 实现 Cordis isolate + 即用即走零残留（ADR-0004）。状态机 `W-xxx`(dispatched→executing→disposed)；`maxConcurrentSubagents` 并发背压；`promoteWorkerOutput` Director 过滤晋升主记忆 fail-closed（FR-10）；`subscribe` 回调供桥消费 inline 芯片事件。
- **T-P2-5 多 Agent 编排（orchdesk-multi）**：8 专家+3 团数据来自插件（EXPERTS/TEAMS，不硬编码主程序）；`getCatalog()` 供渲染层 @modal；`composeTeam()` 经 `ctx.agents.create` 建 CEO→Director(delegationDepth:1)→Worker(delegationDepth:2) 三层后台闭环，finally 中 dispose 即用即走；`getDelegationTree()` 委派树可查询（视觉弱化）。
- 两者均 `inject:['agents']`；`tsc --noEmit` + emit **EXIT=0**（brain/multi 各 EXIT=0，lib/index.js+d.ts 已产出）。

## 关键 dsh API 事实（避免下次重踩，已部分在 skill）
- `ctx.agents.create(opts: CreateAgentOptions): Promise<AgentHandle>`，`AgentHandle={agent, dispose()}`；`CreateAgentOptions={ sessionId: SessionId, meta?:{cwd?,parentSession?,seedLength?,origin?,delegationDepth?,agentPreset?}, seed?, agentOptions?, signal?, setup? }`。
- `SessionId` 是 branded 类型，dsh-session 导出品牌函数 `SessionId(id:string):SessionId`（用 `import { SessionId as brand } from '@deepseek-ai/dsh-session'` 安全构造，勿裸 string cast）。
- `UserMessage` 来自 `@deepseek-ai/dsh-session`（非 dsh-agent）；`Agent`/`PreStepDecision`/`CreateAgentOptions`/`AgentHandle` 来自 dsh-agent。
- 自定义事件跨进程（dsh→桥→渲染）未走 `ctx.emit`（避免 Cordis 未声明事件类型战），改用 `subscribe` 回调 + 桥 seam；`provide?.('brainHands'|'orchestration', api, true)` 可选暴露。

## 门控（与 P1 同源未变）
- 本机 Electron/dsh 运行时阻断（BUG-W02）：T-P2-4/5 的「inline 芯片渲染」「三层闭环后台执行」「委派树可视化」均为运行期验收，受门控；代码逻辑+类型已就绪。
- 运行时插件开关（渲染层 toggle 接真实 Cordis 效应）仍待架构设计（任务卡 #46，需 dsh 控制通道）。

## 文档/审计
- PLAN.md 加 T-P2-4/T-P2-5 状态段；current-state.md P2 进度表加两实现行。
- `audit_knowledge_base.py docs` → files=20 ids=20 issues=0。
- P2 顺序剩余：T-P2-6（OrchClaw Hub）按文档延后，未做。

## P3 安全底座（T-P3-1 / T-P3-2）
- **收敛发现（关键）**：dsh-base 已内置 P3 全部核心，OrchDesk 不重写。
  - 平台沙箱 backend：`dsh-sandbox-local` 的 `PLATFORM_CHAINS` —— win32=`['windows-acl']`（单候选无 probe，enforcement=`partial`）、darwin=`['seatbelt']`（已内置 `seatbeltProfileArgs`，Apple 标记 deprecated 但仍随系统发布）、linux=`['bwrap','landlock']`。
  - `sandbox-policy`（ctx.sandboxPolicy）：`setSandboxMode(session,mode)` + `resolve()`，三档 `SandboxMode`=read-only/workspace-write/danger-full-access（fail-safe 默认 read-only）。
  - `approval` seam（ctx.approval）：`request(req)` 返回 allowed-once/rejected/cancelled/unavailable，**无应答方→unavailable=fail-closed**；`ApprovalRequest={agent,toolName,callId?,reason?,signal?}`；事件 `approval/asked`+`approval/decided` 审计对。
  - `permission` 三 preset 映射（read-only/workspace-write/danger-full-access ↔ sandbox+approval）。
- **T-P3-1**：新增 `docs/30-开发/sandbox-backends.md`（三平台机制+白名单+日志+可转移验证清单 W1-W6）；修正 ADR-0005 macOS 状态（"需自建"→"dsh 已通过 seatbelt 提供"）。运行期受 BUG-W02 门控。
- **T-P3-2 orchdesk-authz 插件**（`packages/plugin/authz/`）：`inject:['sandboxPolicy','approval']` + `provide('authz',api)`。三模式 default/trusted/paranoid 映射 dsh SandboxMode+ApprovalPolicy；L0-L4 分级常量；审批应答方（注册 `approval/request` listener，GUI 弹窗经桥回传，fail-closed 超时/异常/无 UI→unavailable）；审计聚合。tsc --noEmit+emit EXIT=0。
  - `cordis.patch.yml` 加 orchdesk-authz row；bundle package.json 补 `@orchdesk/dsh-authz: workspace:*`。
  - 主进程 `main.ts`：补 IPC `authz:get-mode/set-mode/get-levels/get-audit` + 审批 pending map + `initAuthzBridge(dshCtx)` 接入点（与 runAgentTurn seam 同构，dsh ctx 未接入时 fallback）；`preload.ts` 补同名桥 + `onAuthRequest`/`submitDecision`。tsc EXIT=0。
  - 渲染层 `app.js`：`state.authMode` 改 AuthzMode（default/trusted/paranoid）；composer 授权芯片显示三模式；设置页授权分组补三模式单选+L0-L4+审计日志；审批弹窗 modal+监听（`showApprovalModal`）；`AUTH_MODES` 常量；bridge mock 补 authz 方法（占位环境不崩）。`styles.css` 补 `.auth-modes/.levels/.audit-log`。`app.js` 语法 OK。
- **三模式语义收敛**：OrchDesk 三模式不含 danger-full-access（最松=workspace-write+ask），比旧"FULL ACCESS"更安全；paranoid=read-only+never 最严；从更严切更松需二次确认。
- 文档审计 `audit_knowledge_base.py docs` → files=21 ids=21 issues=0。
- P3 运行期验证（GUI 审批闭环/dsh approval 真实应答/win32 ACL 拦截）受 BUG-W02 门控，须转移机器。
