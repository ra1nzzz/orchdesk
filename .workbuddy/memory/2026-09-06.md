# OrchDesk 2026-09-06 工作日志

## 早上：UI/UX 前端收敛（ui-ux-pro-max SKILL）→ 已提交 5f1ae01 / e59fe5c
对比度过 AA（--fg-faint/--fg-dim）、hardenActions 键盘可达、硬编码色收敛成 token、
插件页 + 技能市场搜索（data-search 即时过滤不触发 render）、设置页导航统一、响应式。
全量 verify 896→907（26 套件）。详见昨日记忆。

## 午后：Skill 发布到本地 + 连接器自动发现
用户裁决先做这两个功能增强。

---

## Skill 发布到本地（零依赖 ZIP 打包器）

### 关键发现：.skill 真实格式
反推本机真实 .skill（promo-video.skill 等）：**ZIP，根目录 = `<slug>/SKILL.md`**（不是
SKILL.md 在 zip 根！），可含 references/ scripts/ 子目录。SKILL.md 顶部 YAML frontmatter
（name/description）`---` 分隔，其后正文。应用内 **.skill 从不被解压执行**——是分发产物，
只做管理（列/删/上传观雅集）。

### skill-pack.ts（纯逻辑，零依赖 STORE zip + CRC32）
不引 adm-zip/jszip（违背零依赖纪律）。STORE（无压缩）+ CRC32（多项式 0xEDB88320）。
输出用**系统 python zipfile 跨语言读回**验证通过（中文/特殊字符保留、testzip 无坏条目）——
手写 zip 最大风险是「自己写自己读都对、别人读不了」，跨语言校验必须做。

### guanji.publishLocalSkill(slug,description,body)
打包 <slug>/SKILL.md → 数据目录 skills/<slug>.skill（临时文件+rename 防半截）。UI：
插件页技能市场加「+ 发布技能到本地」按钮 → 表单(slug/描述/正文) → bridge.publishLocalSkill
→ refreshInstalledSkills。IPC orchdesk:skill-publish-local + preload 桥。

## 连接器自动发现（本地 CLI 登录态）

### connector-discover.ts（纯逻辑）
- parseGitCredentials（~/.git-credentials 行解析 host/user/pass）
- findGithubFromGitCredentials（挑 github.com）
- parseGhHosts（gh hosts.yml oauth 账户，仅作「已登录」信号）
- discoverConnector(github)：git-credentials 明文优先 → gh hosts 备用（token 混淆不可读，
  诚实返回 usable=false + 提示 `gh auth token`）
- 其它连接器（linear/notion/飞书…）本机无标准 CLI 登录态 → 诚实返回「无」，不硬凑

### 诚实性设计
自动发现 ≠ 自动写入：只「找到」secret 回填表单，用户点「保存并测试」才落盘（尊重授权）。
UI 只给 github 显示「自动发现」按钮（其它不展示假按钮）。IPC orchdesk:connector-discover。

### 实机验证发现（重要）
本机 ~/.git-credentials 有**两条** github 凭据，parser 取第一条 = 过期的 x-access-token
（就是 git push 时遮蔽有效 token 的那条！）。发现能命中但可能回填坏 token —— 安全网是
「保存即探测」：保存后真实 GitHub probe 会 401，让用户知道。诚实预填 + 探测兜底是对的，
无需特判。已把此 caveat 记入长期记忆。

## 验证
- skill-pack-verify 10 项（CRC32/slug/frontmatter/跨语言 zipfile 读回）
- connector-discover-verify 10 项（解析/discover 各源/诚实降级）
- e2e 221→225（发布流程 4 条 + 自动发现 4 条）
- 全量 verify 907→935（28 套件）全绿；tsc EXIT=0；arch-guard M4 确认新模块零 electron

---

## 回滚：移除 Skill 发布到本地（用户裁决「暂时移除，保留观雅集下载」）

用户对第二十八批加的「Skill 发布到本地」（表单新建 .skill 打包）方向不合意，要求回滚。

移除范围：
- renderer/app.js：UI「+ 发布技能到本地」按钮/表单、`skill-publish-local` + `skill-publish-local-save`
  两个 event handler、fallback stub
- main.ts：IPC `orchdesk:skill-publish-local`
- preload.ts：`publishLocalSkill` 桥
- guanji.ts：`publishLocalSkill` 方法 + `LocalPublishResult` 类型 + skill-pack import
- **skill-pack.ts / skill-pack-verify.cjs 整个删掉**（只被这个功能用）+ dist 产物
- arch-guard PURE_MODULES 移除 skill-pack.ts、package.json verify 链移除 skill-pack-verify
- e2e：mock publishLocalSkill + 4 条发布断言（225→221）

保留：从观雅集下载 skill（guanji.listSkills+installSkill「安装」按钮）、已安装清单启停/卸载、
`guanji.publishSkill`（发到观雅集，原本就有）。

验证：tsc EXIT=0；e2e 225→221；全量 verify 935→921（28→27 套件，去 skill-pack 10 项）全绿。

**沉淀保留**（写入 MEMORY.md，后续若重做本地打包可直接复用）：.skill 真实格式 = ZIP 根 =
`<slug>/SKILL.md`（非 zip 根），可带 references/scripts/；SKILL.md 顶 YAML frontmatter。

---

## 发版 v0.15.0（打包 + GitHub Release 已发布）

积压 13 个提交（MCP 真接入 / UI-UX 收敛 / 连接器发现 / Skill 发布到本地及回滚）。

流程（遵守 check-version 铁律：tag 必须在打包**之后**打）：
1. bump 0.14.0→0.15.0（**手动改 package.json**，不用 bumpp——它会连 tag 一起打，违反铁律）
2. `changelog.mjs --from v0.14.0 --version 0.15.0 --write`
3. **人工裁决 CHANGELOG**：「Skill 发布到本地」本版内加增删（从未发布），从 Added/Removed 两处剔除，
   避免对外说明出现「加了又撤」噪音；保留存活的「连接器 CLI 登录态自动发现」
4. release commit `74458ee` → tsc + vendor-dsh → 全量 verify 921 项 → kill-running → 打包
5. 打包成功后打 tag v0.15.0 → push main --follow-tags
6. GitHub Release（token 走 API）→ 上传 3 资产 → 补 notes → 转正

产物：Setup 88,120,279 B / Portable 87,775,094 B / latest.yml，输出目录 release-v0150-r2。
校验：sha512 与 latest.yml 一致；asar 203 文件（含 dist/mcp-client.js、dist/connector-discover.js）；
conpty.node 已 unpack 就位。

### 本轮两个新踩坑（已写进 release.md）
1. **打包**：首跑报 `No JSON content found in output`（npm 依赖树收集被污染）→ 换全新输出目录解决；
   随即下载 nsis/winCodeSign **TLS 断连** → 重试循环第 2 次过。延续：换目录 + 重试。
2. **Release（重要）**：API 创建 release 时 tag 未关联 → 建成 `untagged-<sha>`（产物传完才发现）。
   **修正：PATCH {"tag_name":"v0.15.0"} 即可改回，不必删 release 重传 176MB**。
   教训：创建 release 后**先核对返回的 tag_name**，别等传完产物才发现。

---

## 插件页面整体梳理（用户反馈 4 问题 + 全面排查）

用户列出：安装按钮状态不变 / 侧栏有的能点有的不能 / 侧栏 padding 不足 / PRD FR-XX 内部标识。
全面排查后发现比反馈更多的同类问题，一并修复：

1. **技能安装按钮状态化**：恒显「安装」→ 按 installedSkills 判断（已装=「已安装」徽标+「重装」，
   未装=primary「安装」）。新辅助函数 skillInstallCell(slug)。安装流程末尾本就有 render()，改渲染即可。
2. **侧栏导航全整理**：
   - 主区 5 分区加锚点 id（psec-builtin/connectors/mcp/market/temp + psec-skills）
   - 侧栏条目三分类：**pside-nav**（滚到分区+1.2s 高亮 psec-flash）/ **ss-i-action**（带内联按钮如
     派发任务）/ **ss-i-static**（无落点显式标静态：远程未接入、专家名，cursor:default+opacity .72）
   - **market-local-nav 原是空 case（死挂点）**；conn-nav 新增（滚到分区+展开配置，render 后
     requestAnimationFrame 重新滚动——render 重建 DOM 会丢滚动位置）
3. **侧栏 padding**：.ss-i 5px 8px→7px 10px、.ss-h 5px 6px→7px 8px、.ss-l 缩进 20px→16px
4. **PRD FR-XX 清除（UI 可见的 9 处）**：连接器/本地市场（FR-3）、网络域名白名单/沙箱日志（FR-8）、
   授权白名单（FR-9）、分层记忆（FR-10）×2、用量追踪（FR-5）×3、回放数据源（ADR-0009）。
   **代码注释里 68 处引用保留**（可追溯性）。
5. **死按钮/假动作清理**：
   - 「查看审计日志」无 data-action（主进程无审计 IPC）→ 删除
   - 「卸载并回滚」只弹 toast（假动作）→ 改 plug-disable 走真实 setPluginEnabled(id,false)；
     plug-unload 旧入口转发真实停用
6. **e2e 断言假阳性教训**：第一版断言 /已安装/ 匹配全文会撞「已安装（N）」清单标题——改成精确查
   tr[data-skill-search*=guanji] 内「重装」按钮 + 「已安装」徽标。**匹配用户可见状态时必须
   定位到元素级，不能全文正则**。
7. 新增断言：僵尸侧栏条目（无 action 且未标 static）=0、主区死按钮=0、设置页无 FR-\d 残留。

验证：e2e 221→230（+9），全量 verify 921→930（27 套件）全绿；tsc EXIT=0。

---

## 发版 v0.15.1（重新打包，含插件页梳理）

bump 0.15.0→0.15.1（本版只有 fix，按 SemVer 走 patch 而非 minor）。
流程同前，打包**一次通过**（1m52s，新目录 release-v0151-r1）。
产物：Setup 88,121,546 B / Portable 87,776,426 B / latest.yml；sha512 与 latest.yml 一致；
asar 203 文件；conpty.node 已解包。verify 930 全绿 + tsc EXIT=0。
已发布：https://github.com/ra1nzzz/orchdesk/releases/tag/v0.15.1

### 本轮 Release 操作的三个新踩坑（重要，都写进 release.md 了）
1. **重试循环判断写错导致重复创建**：curl 输出经子 shell 格式化后再判断 break，
   而判断读的是原始 `$out`（不含格式化串）→ 条件永不成立 → **创建了 5 个 release**。
   教训：**重试循环的 break 条件必须基于命令真实输出（HTTP code），不能基于下游格式化文本**；
   创建类（POST）操作要先查重或只跑一次，绝不放在「失败就重来」的循环里。
2. **curl 上传中断会留下截断的损坏资产**：curl 返 HTTP 000（客户端没收到响应），
   但服务端已接收部分数据 → 资产存在但尺寸不对（Setup 85,553,915 ≠ 正确 88,121,546）。
   发现方式：上传后**核对每个资产 size 与本地一致**。修正：删掉全部资产用 python 重传。
   教训：**发布前必须逐资产核对 size，光看「上传成功」不够**。
3. **api.github.com 间歇性 000 / SSL EOF（约 50%）**：PATCH 转正要重试 10+ 次才命中。
   上传用 uploads.github.com（python urllib 稳定）。

### 附带发现（未处理，待用户裁决）
GitHub 上**每个历史版本都有 2 个重复 release**（v0.15.0/0.14.0/0.13.2/0.13.1/0.13.0，
各一个 1 资产 + 一个 3 资产，多为 draft）—— 历史流程造成的重复条目，
删除属破坏性操作，已告知用户决定。
