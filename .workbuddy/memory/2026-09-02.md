# 2026-09-02

## PTY 终端 Tab + 只读文件 Tab（Minke 对照 P2-10/11，ADR-0012）收尾闭环
- 全量 `npm run verify` EXIT=0：**745→786 项、21→23 套件**（新增 terminal-pty 26 + file-panel 15）。
- 新增 `docs/70-决策/ADR-0012-terminal-pty-and-file-panel.md`：node-pty 多候选加载（env > vendor > node_modules > dsh profile）+ 管道显式降级（via 可见）；终端洪峰防护（16ms 攒批 / 256KB 截断 / 64KB 回放 / 上限 6）；文件 Tab 只读口径（不走 Agent 授权门、2MB 显式截断、二进制双通道嗅探、语言探测不猜）；shiki 精简 bundle（createHighlighterCore + JS engine，897KB）。
- CHECKPOINT 更新：状态表（最新 Commit `bc07239`、验证 786/786、23 套件）+ 第十五批 blockquote + 页脚。
- feat commit `bc07239`（17 文件，+2511 行），随后 docs commit。
- 待办：真机 GUI 冒烟（终端/文件面板实机点击）须用户桌面会话（BUG-W02 门控）；npm 依赖曾上溯装入 `C:/Users/my/node_modules`（home package.json 污染，shiki/xterm 源所在，后续可移入 workspace）。
- 推送成功：`d0ab400..3661ef9 main -> main`（GitHub TLS 故障持续约 1 分钟，第 8 次重试成功；直连 curl 同期也 000，属网络瞬断非配置问题）。

## P3 文件 Tab 编辑/diff（ADR-0013）
- 全量 verify **786→805 项、23→24 套件**（新增 file-edit 19 项）。
- 新增 `docs/70-决策/ADR-0013-file-edit-diff.md`：**用户亲手编辑不走 Agent 授权门**（Agent 的 file_write 工具链路不变），用四道防呆代替——外部修改检测（expectedMtimeMs + 2ms 容差 → code='modified-externally'）、editable 主进程统一判定（二进制/截断/非 UTF-8 U+FFFD）、临时文件 rename 原子写回、normalizeFileWrite 入口防呆。
- diff = 编辑缓冲 vs 磁盘基线（不做 git diff）；`renderer/file-edit.js` UMD-lite（LCS 前后缀裁剪+DP、groupHunks ±3、**EOL 保护**：textarea 把 CRLF 吃成 LF，写盘前按原风格还原）；5000 行/4M DP 配额 → tooLarge 显式降级。
- 两个真 bug 由新套件首跑抓出：① 回溯循环退出后剩余 add/del 行没吐（单行改动的 add 行消失）；② 完全一致时返回全 ctx 行（「无变更」显示成整文件上下文）。
- commit `a7afcf2`（feat）+ `9444d61`（docs），push 一次成功（`3661ef9..9444d61`）。
- 待办：真机 GUI 冒烟（编辑/保存/对比实机点击）须用户桌面会话。

## 文档防漂移对齐（17:47 批次）
- commit `9f51aa1` 已推送（9444d61..9f51aa1）：canonical 文档全面对齐 2026-09 交付。
- 更新面：architecture（§9 补 ADR-0008~0013 索引 + 新增 §10 桌面壳增强分层）、PRD v1.2（FR-14 桌面工作台面板）、quality-gates（§6 验证套件链结构性约定，计数 canonical 归 CHECKPOINT）、current-state（Minke 对照四批章节 + 关键事实 #3）、roadmap（近期 Minke 状态/中期文件编辑器升级）、60-BUG（BUG-W02 范围收窄：agent 宿主 vs 正常桌面）、README（索引补 CHECKPOINT）。
- 防漂移口径确立：verify 计数最新快照唯一归 CHECKPOINT 状态表；结构性约定归 quality-gates；需求归 PRD FR-14；分层归 architecture §10。
- 环境注意：bash heredoc 写中文内容到文件**字节正常**（Grep UTF-8 语义验证），但控制台显示乱码——验证编码要用 Grep/Read，别信 tail 显示。

## yt-dev-review 三方审阅与交叉修复（22:35 批次）
- commit `2708489`（fix）+ `6473451`（docs）已推送（9f51aa1..6473451，一次成功）。
- 审阅范围：P2-10 终端 PTY / P2-11 文件 Tab / P3 编辑-diff（d0ab400..9f51aa1）。3 个子代理（质量/效率/可复用）并行只读分析，主会话交叉比对后自行修复（跨文件一致性）。
- 修复 14 项，分三类：
  - 质量（9）：ptyAvailable 三态、洪峰封顶缺一次 slice、replayBuf 泄漏（kill+exit 两处）、已退出会话占名额、stdin EPIPE、扩展名 basename（带点目录击穿）、readSync 循环读满、encodingSuspicious 改 TextDecoder(fatal)、EOL 多数派+CR-only、文件面板 bridgeMissing。
  - 效率（4）：shiki 体积门槛 200KB/3000 行（实测 2MB 阻塞 10.5s）、目录不 stat + 扫描量单独计数、dirty 基线缓存、pre 改数组 join + esc 查表外提 + sortTreeEntries Schwartzian。
  - 可复用（1）：新建 common-tools.ts 收敛 clampInt/isAbsoluteLike/extOfName（原 4+4 份），describeTerminalState 接入生产（原为零调用方死挂点），humanSize 成 sizeLabel 唯一真源（删渲染层 fmtSize）。
- 验证：terminal-pty 26→29、file-panel 15→20、file-edit 19→20；全量 verify 805→814（24 套件）全绿；tsc EXIT=0。
- 踩坑：新断言插在用例序列中间会污染后续状态假设（会话数/fake 索引错位）；测干净实例用 require.cache 删除后必须恢复，否则 C 组 require main.js 拿到新实例。
