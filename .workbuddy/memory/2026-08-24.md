# 2026-08-24 · OrchDesk 工作日志

## P5 补偿层 + 自进化（收口）
- 完成 T-P5-1 `orchdesk-compensation` 与 T-P5-2 `orchdesk-evolution` 两个 Cordis 插件（同 P2–P4 同构：`inject:['approval']` + `provide` + `agent/pre-step` + `ctx.approval.request`）。
- compensation：分类（delete-file/external-message/network-egress/shared-file-write/irreversible/other）+ WITHHOLD_CATEGORIES 覆盖三类外发+三类高危；withhold 预判 + compensate 补偿（不宣称可完全撤销）+ 审计环形 200；fail-closed（无 approval 通道→reject）。
- evolution：staticGate 静态分析（HARD_DENY 命中 fail-closed，其余 requiresSandbox）+ createTempPlugin（先静控再 approval CONFIRM，仅驻内存 Map，重启即失）+ disposeTempPlugin。
- 修复：compensation `CATEGORY_RULES` 补齐 `删掉/删去` 与裸 `API`（否则「删掉文件」「调用外部 API」分类失败）；verify-p5 由 16/20 → 20/20。
- bundle 接线：cordis.patch.yml 加 orchdesk-compensation/evolution 两 row；bundle package.json 补 workspace:* 依赖（pnpm-workspace `packages/*/*` 已覆盖）。
- 渲染层 UI（app.js + styles.css）：composer「不可撤销」警示条（实时 bridge.withhold）、设置页「补偿层审计」分区 + 补偿动作按钮、插件页「临时插件」面板（视觉弱化）+ 新建/卸载；bridge 补 6 个 mock 方法（占位乐观本地状态）。
- 验证策略：本机无显示器，故 `scripts/verify-p5.mjs` 用 node 加载真实编译 `lib/index.js` + mock ctx 直驱 `apply()`，绕开 GUI 验证真实逻辑。
- 文档：PLAN P5 8 项验收全勾选；current-state 加 P5 进行中段；知识库审计 status=passed files=21 issues=0。
- 任务卡 #60/#61/#62/#63 → completed。

## 下一步（按 PLAN）
- P6 生态与发布：观雅集技能市场接入（T-P6-1）→ OrchClaw Hub 联调（T-P6-2）→ 打包发布（T-P6-3）。

## P6 生态与发布（收口）
- T-P6-1 观雅集：新建 `apps/desktop/guanji.ts`（`GuanjiClient` 复用 guanji SKILL v2.0.0 API 约定：recommend/latest·featured·top 合并、`<slug>/download`、auth/token、`/upload/prepare`+`/skills/upload` 灵璧预发布+`/alias`）；TOKEN 存 `userData/guanji.json` 不硬编码；`capabilityReview` 强制能力审查（auth=1 无 TOKEN→needs-auth 拒绝，不跳过）。
- T-P6-2 OrchClaw Hub：`apps/desktop/hub.ts`（`HubClient` 配对 `/api/pair` + 发任务 `/api/agent/<h>/task` + 回收 `/api/agent/<h>/result/<taskId>`）；凭据经 `electron.safeStorage` 加密落盘 `userData/hub.json`（无加密后端拒绝明文）。真实 REST 客户端，不在本地 mock。
- T-P6-3 打包：`apps/desktop/package.json` 加 `build`（appId com.orchdesk.app、asar、files=dist+renderer+package.json、win=[nsis,portable] x64、publish github）；凭据在 userData（应用外）不进包。新增 `snapshotData()`（更新前复制 userData→snapshots/<时间戳>）+ `checkForUpdates()`（防御性 `import('electron-updater')`，未发布提示）。
- 桥接：preload.ts + main.ts 补 guanji:{token-status/set-token/list/install/publish}、hub:{status/pair/send/result}、snapshot-data/check-updates；渲染层 app.js 技能市场接真实列表（无 TOKEN 回落静态样本）、安装走能力审查+授权、发布、已安装启用/停用/卸载；Hub 卡片升级配对 UI；设置页「检查更新（先快照）」按钮。
- 验证：desktop `tsc -p tsconfig.json` EXIT=0；`node --check app.js` OK。guanji/hub 因 import electron，运行期需 Electron+网络/远程，与 P1 同源门控（代码+类型就绪）。
- 文档：PLAN P6 12 项验收全勾选 + 状态块；current-state 加 P6 进行中段 + #3 扩生态客户端；知识库审计 status=passed files=21 issues=0。
- 任务卡 #64/#65/#66 → completed。**P1–P6 全部代码与配置就绪，路线图收口；待换可运行 Electron 环境做实跑/联调/打包验证。**

## 提交待办（SOP）
- 按项目 SOP（原子提交 + 三方 review），P1–P6 全部改动待用户授权后提交。

## 提交并 REVIEW（完成）
- **仓库初始化**：OrchDesk 根目录此前从未 git init（仅 references/ 下 vendored 仓库有 .git）。本轮 `git init`（分支 main）+ 仓库级身份（my / my@orchdesk.local）+ `.gitignore` 补 `references/`（vendored 独立管理）与 `.workbuddy/tmp/`（会话临时缓存，含 chrome-profile 会因文件占用致 git add 失败）。
- **首次提交** `b631371` feat: OrchDesk P1-P6 全阶段落地（82 文件）。
- **三方并行 REVIEW**（主进程桥接 / 插件层 / 渲染文档）发现 4 P0 + 8 P1，交叉验证后全部修复：
  - P0 安全：renderer XSS（新增 `esc()` 统一转义消息/工具名/技能列表/hub 结果/侧栏标题）；guanji-publish 路径白名单（仅 userData/skills 内 .skill，堵任意文件外传）；guanji TOKEN 改 safeStorage 加密（无后端拒绝明文）。
  - P0 门控（对齐真实 dsh 契约）：intent/compensation 文本提取 `kind`→`type`（dsh TextBlock.type，原 fail-open）；compensation/evolution `approval.request` 改 dsh 契约（req 必含 agent，signal 入 req.signal）；intent F2 补删除规则 + F4 命中破坏性词表（weight>=0.4）即判不可逆；authz 不再传假 Session（seam 降级为审计+注释）。
  - P1 契约：preload/main 补齐 P4/P5 共 11 个桥方法（占位 + warn 日志，桌面端不再静默失效）；window.prompt→askInput modal（Electron 不支持 prompt）；高危技能安装需显式授权（authorized 参数）；hub URL 强制 https（本地回环例外）；initAuthzBridge 启动调用（消死代码）；evolution HARD_DENY 加固（bracket/global/constructor 绕过）；prompt resolveBody 支持异步 resolver；trace 加 30s 定时 flush。
  - 文档：**BUG-W02 结案**（真相：误用 `node` 跑主进程，正确 `electron .`；本机仅剩无显示器门控）；PLAN v1.2 验收勾选口径 + P0/P1 文档结构类勾选；current-state/60-BUG 口径统一。
- **修复提交** `de10f0c` fix: REVIEW 三方审计修复（16 文件 +412/-138）。
- 验证：9 插件与 desktop tsc EXIT=0、`node --check` OK、**verify-p5 23/23**（新增 ContentBlock 结构/契约违约/缺 agent 防回归用例）、知识库审计 files=21 issues=0。工作树干净。
- **经验**：① Cordis 插件对接 dsh 服务前先核对 `references/deepseek-harness/packages/*/lib/types/*.d.ts` 真实签名（TextBlock.type、ApprovalService.request(req 含 agent/signal)、setSandboxMode(session,mode)），mock/占位易掩盖契约错误；② verify 脚本 mock 应校验请求形状，否则真实 dsh 下才暴露；③ `git add` 遇 .workbuddy/tmp 文件占用（Cookies）会整体失败，须先排除。
