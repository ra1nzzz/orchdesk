# 2026-08-23 工作记录

## P4 智能层收口
- 新建 `packages/plugin/memory/`（orchdesk-memory，`inject:['agents']` + `provide('memory')`）：80% 上下文阈值转储 + 本地 TF-IDF 召回（不云端）+ 四域（global/project/director/worker）物理隔离 + 晋升流 fail-closed（worker→director 经 `brain.promoteWorkerOutput`）。
- 新建 `packages/plugin/prompt/`（orchdesk-prompt，`provide('promptLib')`）：CRUD + 分类标签（角色行为/安全边界/输出格式/技能联动）+ `{skill:xxx}` 引用语法解析 + 按 Agent 绑定 + 优先级合并（冲突显式标记）。
- bundle 接线：`cordis.patch.yml` 加 `orchdesk-memory`(L50)/`orchdesk-prompt`(L59) row；bundle `package.json` 补 `@orchdesk/dsh-memory`/`@orchdesk/dsh-prompt: workspace:*`。
- 渲染层 `app.js`：设置页「系统提示词」分组 + 侧栏「提示词」导航 + 桥方法 `listPrompts/mergePrompts/savePrompt/deletePrompt/getMemoryStats`（占位 mock + 乐观本地更新，真实 IPC 待接 dsh ctx）；`app.js` 语法 OK；`styles.css` 补 `.prompt-list/.pl-item/.mb-row`。
- tsc 全绿：memory/prompt 插件 + desktop 主进程/preload/渲染层均 EXIT=0；`node --check app.js` OK。
- 收敛事实：dsh 无 memory/vector/context-window 专属包 → memory-layers/prompt-lib 是 OrchDesk **自建业务插件**（与 P2/P3 同构，薄封装 dsh 既有 `agent/pre-step`/`ctx.agents`）。
- 门控：BUG-W02（本机 Electron 运行时不可跑），真实 IPC 桥（preload→主进程→`ctx.memory`/`ctx.promptLib`）为与 P1 同源 seam（任务卡 #46）。

## 任务卡
- #58（T-P4-1/2 memory）completed；#59（T-P4-3 prompt）completed。
- PLAN.md / current-state.md 已加 P4 状态段。

## 下一步候选（不超范围，待用户选）
- P5（补偿层 + 自进化）按路线图；或先写 P4 验收脚本（转储/召回/四域隔离/提示词合并冲突的运行期验证，需可运行 Electron 的机器）。
