# 2026-09-03 工作日志

## OpenWorker 竞品对照分析（临时目录拉取，未入库）

- 快照：`github.com/andrewyng/openworker` @ `fb1bfc6`（623 文件；Tauri2 + React18 + Python FastAPI sidecar 127.0.0.1:8765，底座 aisuite）。克隆到临时目录供分析，**代码不入库**（知识库约定：外部资料只引用不回拷）。
- 方法：3 个并行 Explore 子代理分头深挖（治理安全层 / agent 内核与能力层 / 前端与工程化），主会话读 OrchDesk 的 PRD + PLAN + current-state + roadmap + CHECKPOINT 做对照。
- 产出：`docs/00-项目/openworker-对照-2026-09-03.md`（id orch-cmp-001），已挂 docs/README.md 索引，audit 0 issues。

### 结论要点
- **同构度高、治理路线相反**：OpenWorker 把治理当产品（三层自治阶梯 + 硬地板 + reviewer 模型 + 审计 provenance），OrchDesk 把治理当插件（authz 挂 dsh seam）。治理深度差距明显，定向搬 8 条（A 档）。
- **A 档（低成本、建议近期）**：审批项幂等落盘（我们 `main.ts:1890` 是内存 Map+timer，重启即丢）/`human_only` 决策位/override 只能收紧/风险改工具声明属性（现分散在 executeTool 各分支）/中断拒绝必补 tool-error/审批 provenance 三落点/流文本三态闸门（40 词阈值纯函数）/**verify 环境强制隔离**（`tests/conftest.py` 用 autouse fixture 重定向 state dir，我们缺失，有污染真实 state 风险）。
- **B 档（需设计）**：SKILL.md 文件化+渐进披露 / persona manifest 文件化+角色即工具集 / 看板=append-only 事件日志+投影 / feed 兴趣订阅 / **压缩补机械 working-state 提取**（纯 LLM 摘要必丢「改过哪些文件、跑过什么命令」）/ MCP 客户端 / 四级授权递进（PRD FR-9 要三粒度，实现仅单次）。
- **不抄**：reviewer 第二模型裁决+断路器、Inbox/Slack 镜像、shell 静态只读分类器（350 行白名单）、React+Vite+Tailwind 构建链、68 个 Playwright spec 的规模、workspace_trust 哈希链。
- **最大欠账不是代码是勾选**：PLAN 107 项验收中 **23 已勾 / 84 未勾**，但 BUG-W02 已于 09-01 收窄（用户桌面可实跑、browser smoke 11/11），所以 P0–P4 的 83 项运行期验收**不是环境阻断，是待执行待回勾**。

### 顺手修掉的文档漂移（4 处）
1. `current-state.md` 验证规模：805 → 补成 313 → 805 → **814** 演进链（CHECKPOINT 是 canonical）。
2. `roadmap.md`：805 → 814 并指向 CHECKPOINT。
3. `quality-gates.md §6`：标题自称「canonical 计数方：本节」与「计数唯一归 CHECKPOINT」**责任冲突** → 改为「canonical 计数方：CHECKPOINT，本节不复制计数」，删过期数字。
4. 发现但未改：`PLAN.md` P0–P4 状态块仍用「BUG-W02 门控」旧口径（需先组织实跑清单，改口径才有意义）——记进报告 4.1 与下一步排序。

---

## v0.13.0 打包执行（2026-09-03，用户指令「打包执行，先实跑」）

### 执行顺序（已验证可复用）
1. **先实跑**：verify 链 24 套件 → **814 项全绿** EXIT=0。坑：e2e 那行输出是 `📊 结果:`（带 emoji），`grep "^结果"` 只数到 23，误以为漏套件。
2. changelog → bump → release commit → **打包** → **最后打 tag**。
3. **tag 必须在打包之后打**：`check-version.cjs` 严格模式下 `version == 最新 tag` 会阻断 `dist`，先打 tag 会自己卡死自己。
4. 打包命令：`env -u NODE_OPTIONS -u ELECTRON_RUN_AS_NODE -u <代理全家> NODE_OPTIONS="--use-system-ca" npm_config_safe_delete=false ./node_modules/.bin/electron-builder --win --publish never`（绕开 pnpm 包装层触发的 BUG-W01，保留系统 CA 供下载）。

### 踩坑：EBUSY asar 句柄泄漏（新，之前没记过）
- 现象：`kill-running.cjs` 报「无运行中的 OrchDesk 进程」，仍 `EBUSY: unlink .../app.asar`，删除/改名/等 30s 全不行。
- **根因：electron-builder 报错后进程不退出**（`npx electron-builder` + `cli.js` 两层 node 都挂着），持续持有刚写入的 asar 句柄 → 此后所有重试被同一句柄拦死。
- **解法：换全新输出目录** `-c.directories.output=release-v0130-r1`。同目录重试 5 次全失败，换目录首次即通过（1 分钟）。
- 排查必须按**命令行**找进程（都叫 node.exe，按进程名找不到）：`Get-CimInstance Win32_Process | ? CommandLine -match electron-builder` → `Stop-Process -Force`。
- 别误判成沙箱拦截：同目录「新建文件再删除」成功 = 目录无保护，是**特定文件**被锁；与后缀、大小、目录位置均无关（逐一对照验证过）。
- 附带：直连 GitHub 取 nsis/winCodeSign 约五成概率 TLS 断连 → 循环重试兜住。

### 产物与校验
- `OrchDesk Setup 0.13.0.exe` 88,081,451 B / `OrchDesk 0.13.0.exe` 87,736,328 B / blockmap / latest.yml（已归位 `release/`）。
- Setup sha512 `OHBIBXMkMLufCVOjfrmocL7RM5Qg6ehoDziyPK/0PLjS+hauXxap9SlETqpFn26HwVGYl9VNKkKsOn1pZsk9bw==`，与 latest.yml 一致。
- **asar 校验**：195 文件，`dist/main.js` / `dist/preload.js` / `renderer/app.js` / `renderer/file-edit.js` / `renderer/vendor/shiki-bundle.js` / `renderer/vendor/xterm/*` 全在包内。自写解析脚本注意 asar 头部是 **pickle 格式**（前后都有填充），从第一个 `{` 截到最后一个 `}` 才 parse 得动。
- **node-pty 1.2.0 走 prebuilds 机制，不是 `build/Release`**：`vendor/node-pty/build/Release/` 下没有 .node 属正常，别误判缺失；要验的是 `app.asar.unpacked/vendor/node-pty/prebuilds/win32-x64/conpty.node`（291,328 B）。asarUnpack 覆盖 `vendor/node-pty/**` 即可。

### 实跑清单（本轮另一半交付）
`docs/40-质量/smoke-checklist.md`（id `orch-qag-002`），A–F 六组 + 三条硬口径（降级不算通过 / 未接入≠为空 / 不为全绿放宽预期）。**这是 PLAN 84 项未勾的解锁钥匙**：BUG-W02 已收窄，83 项运行期验收是**待执行**不是被阻断；文档里「受 BUG-W02 门控」是过期口径（已在 current-state 显式修正 + 指向 60-BUG）。

### 待办（更新）
- 用户桌面跑实机冒烟清单 → 回勾 PLAN → 推 v0.13.0 Release。
- 清理被锁的 `release/win-unpacked`、`release-v0130*`（重启后可删）；`release-v0130-r1` 是本次成功产物目录。
- A 档 A1/A4/A6（审批落盘 + 工具风险元数据 + provenance）、A8 verify env 隔离。

### 长期记忆整理（收尾）
- `MEMORY.md` 从 9,592 B / 5,918 字压到 ~3,800 字：删掉与 ADR/docs 重复的实现细节，只留「不可复现的坑 + 铁律 + 债务」；已进 ADR 的内容在记忆里只留索引式一句（如 0011/0012/0013 只保留挂起源、env 剔除、LCS 回溯三条）。
- 并入本轮新坑：**tag 必须在打包之后打**（check-version 严格模式会自锁 dist）、asar 句柄泄漏 → 换全新输出目录、node-pty 1.2.0 走 prebuilds、asar 头部 pickle 截取。
- 文档审计 `status=passed files=36 ids=27 issues=0`。**审计脚本在 `.workbuddy/tmp/skills/inspect_consolidate-project-knowledge-base/scripts/audit_knowledge_base.py`** —— 仓库 `scripts/` 下没有副本，别去那儿找；跑法 `env -u NODE_OPTIONS -u PYTHONPATH "C:/Program Files/Python312/python.exe" <脚本> docs`。

## BUG-022 修复（实机冒烟首批反馈）

- **现象**：用户实跑 v0.13.0-r1 发现——新建项目绑定 D 盘目录后，项目 ··· 菜单「打开项目目录」弹出的是 C 盘 `%APPDATA%/OrchDesk` 数据目录。
- **根因**：`orchdesk:open-project-dir` handler 恒 `shell.openPath(dataDir())`，注释「项目目录 = userData」是早期概念混用遗留；项目对象 `path` 字段有写入方、无读取方（渲染层早已用 `p.path` 渲染图标）——死挂点变体。e2e 之前测不到：bridge mock 无条件返 ok + 种子项目不带 `path`。
- **修复**（main.ts / preload.ts / renderer/app.js）：handler 接受可选 `projectPath`（有绑定 → statSync 校验目录真实存在 → 开绑定目录；无绑定 → `{ok:false, reason:'该项目未绑定本地文件夹…'}`）；`shell.openPath` 返回值不再被吞（成功返回 ''）；渲染层传 `p.path` + toast 回显实际路径；顺手修同族 `open-log-dir` 吞失败返回值。
- **验证**：e2e 新增 6 条断言（152→158，mock 用 `window.__openedPath` 逐调用记录参数），verify **24 套件 820 项全绿**，tsc EXIT=0，审计 0 issues。
- **文档**：60-BUG 归档 BUG-022（v0.13.1 pending）、CHECKPOINT 第十九批、release.md +v0.13.1（pending）段、current-state 下一步更新。
- **教训**：mock 太宽容（无条件 ok、种子数据缺字段）会让「传参错误」类死挂点测不出来——回归断言要让 mock 逐调用记录参数。

## v0.13.1 发版（BUG-022 落包）

- 顺序：changelog 0.13.1 → bump package.json → release commit `09784ba` → tsc + vendor-dsh → 打包（全新目录 `release-v0131-r1`，一次通过）→ tag `v0.13.1`（打包后打）→ push main + tag。
- 产物：Setup 88,082,407 B / portable 87,737,276 B，sha512 `ItSWEaKXFiIAOjXzc0o8pz13YacF1/K6ZqosdXKJA6KetafYHOIS33pJ2Q2uAOSRDYUtWvzV58PN/vAm34pvVg==` 与 latest.yml 一致。
- 网络：推送连续 10 次 TLS 握手失败后第 11 次成功——抖动加剧时重试循环要给足（≥10 次、间隔 8-12s）。
- 文档：release.md / CHECKPOINT / current-state 同步至 0.13.1 已打包态；GitHub Release（v0.13.0 + v0.13.1）待冒烟后转正。

## BUG-023 会话工作区贯通（实机冒烟第二批反馈）

- **现象**：指定项目目录的新会话 cwd 仍是 user home（git pull 报「C:/Users/my 不是 Git 仓库」）；重选项目不切换；Agent 自己 set_cwd 到 D 盘也被沙箱拒。
- **根因**：项目 `path` 只在渲染层，主进程 `sessionCwds` 只由 set_cwd 工具写入——项目绑定目录到会话工作区整条链路从未接通。三处断点：无贯通 IPC / isPathAllowed 白名单不含工作区 / 五个驱动点都没调。
- **修复**：新 IPC `orchdesk:set-session-cwd`（GUI 绑定不走授权门，严格校验绝对路径+存在+目录）；isPathAllowed 纳入 sessionCwds 根；渲染层 applySessionCwd 五驱动点（创建/打开(幂等)/重选项目/home发送/分叉）；文件面板缺省根 + 终端缺省 cwd 落项目目录（目录已删→主进程删 cwd 字段回退，可见）。
- **验证**：model-loop B1–B5 + e2e 7 条，verify 24 套件 832 项全绿，tsc EXIT=0，审计 0 issues。
- **教训**：BUG-020 修了「Agent 主动切目录」没修「项目自动切目录」——工具有了 ≠ 链路通了，同类死挂点要把所有驱动点都走到。e2e 写回归的两个坑：前 13 组用例会删种子会话（回归块要放 reload 后）；toggle 展开会破坏后续用例的「默认折叠」假设（用完还原）。

## v0.13.2 发版（BUG-023 落包）

- 顺序同 0.13.1：changelog → bump → `eb57ce2` → tsc+vendor-dsh → 打包（`release-v0132-r1` 一次通过）→ tag `v0.13.2` → main+tag 一次推送成功。
- Setup sha512 与 latest.yml 一致（`jfG1ccbTOJiYtlCQJwho…`）。
- 待办：三个 GitHub Release（v0.13.0/1/2）待实机冒烟后转正；v0.13.2 桌面复验点 = 新会话默认 cwd=项目目录、重选项目即切换、git pull 落项目目录。

## 全盘死挂点扫描修复 + 三方审阅（任务 #44–#47）

- 修复落点：main.ts 5 个死挂点 + `rendererWindow()` 收口 4 份窗口选择副本；app.js 6 个死挂点（含 askInput 假 Promise、图标表补 barChart/grid）。commit `269b62a` 一次推送成功（42dac2c..269b62a）。
- 四张全景图死挂点归类：**发向死窗**（notifyToolStep 取 getAllWindows()[0]，悬浮窗排第 0 无 preload）、**只写不读**（onToolStep 订阅写入 state.toolSteps 但零读取=存而不显）、**静默 fail-open**（outboundGate 无服务放行无日志）、**参数漏传断链**（文本兜底模式漏 sessionId → set_cwd/grant/沙箱日志整链断）、**接口假 Promise**（askInput 是 onOk 回调式，team-compose 对 undefined .then 必崩）、**引用未定义**（iconMap 的 bar-chart/grid 图标从未进 I 表 → ic() 渲染空 SVG）。
- 三方审阅（yt-dev-review）交叉修复 6 项：抽 `rendererWindow()` 收口 main.ts 4 份窗口选择副本（原来注释还写「同款」）；抽 `toolRow()` 统一 live/静态双份工具行模板 + error 红点语义；停用插件 error 置「已停用（逆回滚完成）」被误标「异常」→ 按前缀区分；doSend 成功/失败路径都 delete toolSteps 防驻留；onToolStep 每事件全量 render() 无节流（文本兜底批量连发 2N 次/回合）→ 150ms 尾沿合并；注释去掉行号互指（防漂移）。
- 验证：tsc EXIT=0；verify 24 套件全绿（退出码 0，e2e 165）；node --check 通过。**未改任何 docs，无需知识库审计。**
- 遗留（未采纳为债务，记此备查）：① 渲染层 pluginBadge(412)/pluginSwitchedOn(430)/ctx rtOf(1313) 仍有 3 份 rt.plugins.find，可上收 `rtPlugin(name)`；② 任务跟踪面板历史 m.tools 每 render 全量建行，长会话可截断前 K 行；③ #48 浏览器自动化全面 e2e 仍未做（需真机 GPU，BUG-W02，待用户桌面会话）。

## 浏览器自动化 e2e 组 15 死挂点回归（任务 #48，收口）

- 此前 #44–#47 遗留③ 误判「需真机 GPU」：实际 headless Chromium（Playwright）走 file:// 加载渲染层 + mock bridge 即可覆盖 DOM 路径，无需 exe/GPU。
- `e2e-fix-verify.cjs` fixture 补三处：`onToolStep` 订阅回调留痕 `window.__toolStepCb`；`getPluginRuntime` 按 `localStorage.__rt==='1'` 返回就绪+4 插件状态；新增 `composeTeam` 调用留痕 `window.__composeCalls`（返 3 节点委派树）。
- 组 15 共 27 项断言，6 条链路全用真实渲染层复现：① live 工具步骤（deferred runAgentTurn 接管 → push running/done → typing 实时行 → 回合结束静态落库第二个 details）；② 静态 tools「N 步 · M 个动作」+ 明细展开；③ 设置 statbar 真实目录末两段 + 运行时就绪 3/4（不再硬编码 %APPDATA%/dsh）；④ 任务监控「技能与MCP」状态标注（trace 主动停用=已停用 ≠ brain 真异常）；⑤ 专家团派发 askInput 回调式（不再 .then 崩溃）+ composeTeam 真被调 + 委派树 3 节点；⑥ 文件面板 #fileRoot hidden 开合。
- 验证：e2e 165→192，全量 verify 24 套件 859 项退出码 0。
- docs 同步：CHECKPOINT 行 19 计数 + 第二十一批记录 + 最后更新；current-state.md 补 #48 条目与 updated 日期。MEMORY.md 已瘦身（6180→~4.8KB）防注入截断。

- **收口推送**：`8701b36`（test 组 15）+ `ab3da9a`（docs 收口）直连一次推送成功（933e511..ab3da9a main -> main），#48 标记 completed，工作区干净。

## 全项目多维代码审验（code-review-and-quality skill）第一轮修复

- 安装「多维代码审验」skill（用户级 ~/.workbuddy/skills/code-review-and-quality，slug code-review-and-quality，纯文档方法论）；guanji 升级 v2.0.0→v4.1.0。用临时 TOKEN 01M1…（ra1nzzz）下载，换取持久 TOKEN 沿用 GUANJI_TOKEN（01KZN…，30 天）。
- 方法论：五轴（Correctness/Readability/Architecture/Security/Performance）+ 严重度分级 + 结构修复导向。三路只读 Explore 盘点 main.ts + 工具运行时层 + 插件/渲染层。
- **本轮已修（确定性高危，全绿验证）**：
  1. guanji.ts installSkill：slug 过 isMarketDirName 白名单（防目录穿越任意写）+ .skill 体积上限 64MB（content-length+实读双保险）。
  2. main.ts file_read：stat 预检，>2MB 直接拒（原整读 10GB 阻塞主进程）。
  3. main.ts web_fetch：content-length 预检 + 流式读满 1MB 即停（原 res.text() 整读）。
  4. main.ts 删孤儿 `orchdesk:memory-recall` IPC（渲染层零引用，主链路召回在 runAgentTurn 1345 未动）；`orchdesk:tool-execute` 经核实是 browser-tools/credentials-verify 的测试后门 → 保留+标注"测试专用勿接 UI"（修正 Explore 误判"死 handler"）。
  5. agent-runtime.ts normalizeNativeToolCalls：畸形 primitive function 用 `in` 会抛 TypeError → 收窄对象再访问 + 顶层 input 三态兜底（与 wrapper 分支口径一致）。
  6. common-tools.ts 新增 toBool（Boolean('false')===true 陷阱收敛真源）+ browser-tools fullPage/clear/pressEnter 改用。
- 验证：tsc 0 err；全量 verify 24 套件退出码 0（e2e 192）。新增回归断言 agent-runtime +2、browser-tools +1。教训：改 TS 后 verify 跑 dist 产物，必须 npx tsc 再跑（否则假结果）；删 IPC handler 前先 grep verify 层引用（tool-execute 是测试后门）。
- 待裁决架构项（未动，需用户定夺）：① IPC 全 90 处无 event.sender 校验 + 窗口无 will-navigate 防护；② 半接线能力（brain dispatchSubAgent/directorFilter 零消费者、multi composeTeam 空转、agents.followup 零调用、app.js sim-subagent 假芯片）；③ 无界内存队列（trace pending / memory dumps / multi tree / sessionIntent）；④ 插件 canonical 重复（4 份文本提取 / 3 份风险分类器 / 4 份 ApprovalOutcome / 5 份 id 生成器）；⑤ dsh-runtime 662 行职责>4 可拆 + 插件装载三样板 + startRuntime 并发竞态；⑥ loadModelConfig 默认分支 20/200 不一致。
