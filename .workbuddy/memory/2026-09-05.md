# 2026-09-05 工作日志

## UI 重构四连（需求1-4）+ impeccable UI/UX 审查（需求5）

用户一次给了 5 项要求，前 4 项是 UI 重构、第 5 项是审查。用户裁决三个方向性问题后自主推进完成。

### 裁决结果（AskUserQuestion）
1. 浏览器「多TAB」→ **登记页面快照 TAB**（底层 `browser-cdp.ts` 只有 `let win: BrowserWindow`，
   单窗口无多页面概念；不做真多页，卡片表达「访问过的页面」）
2. 待办语义化 → **两者结合**（结构化 plan 块优先 + markdown 列表回退）
3. 文件 TAB 目录 → **自动跟随项目目录**

### 实现（commit `6447968`，verify 875→877）
- **需求1 待办语义化**：旧实现直接把 `m.tools` 工具名当待办 → 列表全是 file_read/shell_command。
  新增 `extractPlanSteps()`：① ```orch-plan / <plan>``` 结构化块（支持 `- [x]`）
  ② 回退解析正文 markdown 列表（checkbox 优先，其次「计划/步骤」小节下）。
  **完成度只用显式标记判定，不拿工具数猜进度**；工具调用降为折叠的「执行明细」。
- **需求2**：删标题栏「深色」按钮（左下角已有 toggle-theme）。
- **需求3 文件→右栏 TAB**：与「产物」不同源（产物=本会话生成，文件=工作目录全部文件）；
  root 自动取会话绑定项目目录；侧栏只浏览，点文件进全屏面板预览。
- **需求3 浏览器→侧栏**：标题栏「浏览器/终端/文件」三个文字按钮全移除；
  状态栏右下角新增图标区（未激活灰/激活点亮/点击切换）；
  浏览器侧栏与任务监控**共用第 4 列**（`.app.browser-mode` 顶替 context），
  Agent 首次调用浏览器自动展开（只在 open 跃迁上自动，避免用户收起后被推送弹回来）；
  主进程新增页面登记簿 + `BROWSER_PAGES_MAX=12` 上限 + 缩略图回填。
- **需求3 终端→底部抽屉**：从 `position:fixed` 全屏覆盖层改为**文档流**抽屉，
  展开即挤压上方主区（`app` 是 flex:1），全屏档位 `calc(100vh - 66px)` 同理；
  高度变化后 xterm 必须重新 fit。
- **需求4 思考链展开**：typing 消息右侧箭头，默认折叠（只显示最近一步），展开看完整明细。

### e2e 回归与修复（195→197，中途 3 次失败全修）
1. **标题栏按钮被删** → 542 行主题按钮断言改为「标题栏不再重复摆」+ 状态栏图标数断言（+2 项）
2. **ctx-tab 用 `nth(2)` 索引定位** → 插入「文件」TAB 后静默切错页，MCP 指示器 count=0。
   **改成 data-id 定位**（抗 TAB 重排）
3. **文件 TAB 在「目录读取失败/未绑定」分支没有全屏入口按钮** → e2e 点不到 `file-panel`
   （file:// 预览下 bridge 不可用必然走 error 分支）。三个分支**都要**有全屏入口——
   目录读不到时用户恰恰最需要它
4. **typing 文案**改了（带工具计数）→ 断言放宽为 `/正在执行.*工具/`
   （核心是 live 轨迹被真实读取，不是固定文案）

**教训**：e2e 里 `ctxTabs.nth(N)` 这类**按索引定位**在 TAB 增删时静默切错页，
排查成本高（表象是后续元素超时）。已统一改 data-id。

### 需求5：impeccable skill 安装 + 审查
- `impeccable` 不在 `~/.workbuddy/skills/` 但**在 `~/.workbuddy/skills-marketplace/skills/` 缓存里**
  （31 个纯 md、零脚本 → P2 安全）→ `cp -r` 装上即可，不用去观雅集/GitHub 搜
- 审查产物：`docs/40-质量/ui-ux-audit.md`（**仅审查，不改代码**）
- **总分 11/20（Acceptable）**：A11y 1 / Perf 2 / Theming 3 / Responsive 2 / Anti-Patterns 3
- P1 ×2：① `--fg-faint` 浅色主题对比度 **2.33:1**（AA 要 4.5:1，被 67 处 10-11px 小字使用）
  ② 全项目 **tabindex 0 处**，`div + data-action` 交互元素键盘不可达
  （既有 `.sess/.proj-head` 补过 focus-visible，但焦点样式存在≠焦点可达）
- P2 ×3：`.term-body` 在 light 分支硬编码 `#161622`（token 破口）、`render()` 全量重绘三块 innerHTML、
  token 块外 6 处硬编码色
- P3 ×2：0 个 media query 无最小宽度保护、67 处 10-11px 小字
- **系统性根因**：① `div + data-action` 模式缺框架层无障碍兜底（应事件委托统一补 role/tabindex+键盘）
  ② 层级区分过度依赖对比度而非字号/字重 ③ token 体系有建但无强制力
- **正面**：无 AI slop（配色克制、无渐变滥用、字体系统原生）；降级可见性贯彻得好

### 验证
- tsc EXIT=0；全量 `npm run verify` 25 套件 **877/877**（e2e 195→197）
- 知识库审计 0 issues；CHECKPOINT 验证套件计数 875→877 已同步
