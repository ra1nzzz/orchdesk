# 2026-08-30

## BUG-018 修复 + v0.4.1 发布
- 修复 StepFun 等网关「发消息能响应，要求执行任务时报模型返回空内容」问题。
- 根因：chat 模式下带 tools 参数返回 HTTP 200 空内容（软拒绝），原降级逻辑只处理硬 4xx。
- 改动：`apps/desktop/main.ts` 识别软拒绝并逐级降级；`runAgentTurn` 用 `Map<provider.id|model>` 持久记忆；`model-loop-verify.cjs` 新增 M 组 3 项回归。
- 验证：`npm run verify` 11 套件 313 项全绿；tsc EXIT=0；知识库 audit 0 issues。
- 版本：package.json 0.4.1，CHANGELOG 已更新，git tag `v0.4.1` 已推送（commit `85ceb46`）。
- 推送：因 HTTPS_PROXY=127.0.0.1:7897 失效，使用 `git -c http.proxy= -c https.proxy= push` 绕过后成功。
- yt-dev-review：三维度并发审阅后发现 toolsRejected 应跨会话持久化、空响应不应误置 toolsRejected、软拒绝分支缺日志，均已采纳修复。

## BUG-W04 修复（ponytail 模式）
- 根因**推翻旧归因**（非 Node 22 / 非历史重建）：`git-raw-commits@5.0.1` 输出不再带 `-hash-` 分隔符（实测 274 行命中 0 次），而 `conventional-commits-parser@6.4.0` 仍靠它切分 → 整段历史被吞成一条 `chore:` 提交 → angular preset 过滤 → 退出 0、产出 0 字节。非版本漂移（core v8 本就声明 ^5.0.0），是上游 v5/v6 迁移的契约断裂。
- 修复：新增零依赖 `scripts/changelog.mjs`（git `--pretty` 用 `\x1f`/`\x1e` 自切分 + Keep a Changelog 分组 + SemVer 推断 + 幂等写入 + `--selftest`）；`package.json` 的 `changelog` 脚本切到它。
- selftest 在开发期即抓到真 bug：`rank['']` 为 undefined 导致 `2 > undefined` 为 false，bump 永远算不出。另修 UTC 日期导致东八区差一天。
- 教训：conventional-changelog 生态 v5/v6 的分隔符契约已变，别再依赖它；自己切分更可控。

## ponytail audit（全仓过度设计审计，只读出清单未改码）
- **验证脚手架重复 ~250 行（最高价值削减项）**：① electron stub 5 份（agent-loop 56-80 / data-port 36-77 / dsh-runtime 43-64 / model-loop 57-81 / credentials 90-101 + 子进程内联 201-214），其中 4 份顶层键完全一致（app/ipcMain/BrowserWindow/Tray/Menu/nativeImage/shell/safeStorage/dialog）；② check()+计分样板 10/11 个验证脚本各自实现一份。建议提取 `scripts/verify-kit.cjs`（stub 工厂 + 计分器）。
- **main.ts:48-49 过时注释**：仍称 run-agent-turn 为「本地占位」，实际早已直连 OpenAI 兼容/Ollama 真实循环（dsh 运行时另走 9 插件服务）。零风险删除项。
- **审计环形缓冲重复 —— 建议不动**：authz:154-155 / compensation:166-167 / evolution:164-165（后两者逐字相同 AUDIT_CAP=200）。跨包提取需新建共享包（package.json+tsconfig+9 处依赖），成本 > 4 行重复的收益，记为可接受债。
- **readJsonFile 两处不合并**：main.ts:1292（生产带 try/catch 容错）vs data-dir-verify.cjs:82（测试裸解析、靠抛错断言）。合并会削弱断言。
- **好消息**：99 个导出符号无一为死代码；全仓几乎零注释废码（仅 1 处且为解释性说明）。

## v0.5.0（dsh memory 接入对话流 + 会话工作目录）
- 用户实测报两个 BUG：①「你叫小星」下一轮就忘（BUG-019）；② D:\Code\WxTools 被判非 git 仓库（BUG-020）。共同根因 = runAgentTurn 是裸模型循环，dsh memory 服务从未接入、无会话工作目录概念。
- 修复：工具 5→7（+memory_save 写 memory 插件 global 域 / +set_cwd 会话目录，进程内 Map）；每轮 listDomain('global') 召回近 10 条注入 system prompt；shell/file 相对路径以会话目录 resolve；提示词声明当前 cwd 并要求「告知事实必须 memory_save，不许口头答应」。
- verify 扩至 11 套件 318 项（agent-runtime 37 / model-loop 31，N1-N3 端到端：保存→下一轮注入→相对路径基准）。测试教训：工具数断言硬编码 5 的有三处（A2/D1/agent-loop），加工具时全要动——下次写成 `TOOL_DEFS.length` 动态引用。
- 发布：changelog 脚本自产自销成功（自动推断 0.5.0 并写入）；package.json 0.5.0 + tag v0.5.0；push 直连失败、**走代理 127.0.0.1:7897 成功**（BUG-W03：直连/代理哪个可用轮换，两种都要试）。
- DSH 深度接入路线（未做，说明口径）：runAgentTurn 直连 OpenAI/Ollama 循环已承载工具/降级/记忆，dsh memory（FR-7）与 authz 已接入；完全切到 ctx.agents.followup 需 dsh 侧 LLM 配置体系，收益/成本不匹配，列为后续。


## v0.6.0（ADR-0008 漂移裁决：intent/trace 桥接主链路）
- 用户质询「DSH 作为 agent runtime 是不是 PRD/PLAN 设定」——查实 PLAN T-P1-5 明文要求接入 `ctx.agents.followup`，实际直连且无裁决 = 漂移成立；更深发现：intent/trace 挂 `agent/pre-step`，该事件只有 dsh AgentLoop 才发 → **主会话上两个 PRD 亮点一直是死挂点**。
- 裁决（ADR-0008 + conflicts C6）：followup 是 fire-and-forget inbox 驱动（dsh runtime-types L124），与 Electron IPC 请求-响应不同构；v1「挂点桥接」= `dsh-runtime.firePreStep()` 每回合驱动 waterfall（`ctx.waterfall(event, payload, baseline)`，真实 cordis 实测可用），reject 硬拒不调模型；AgentLoop 全量事件化列 P7 路线图。
- fail 边界：runtime 未启动/waterfall 异常 → 放行 + WARN（基础设施缺失≠风险放行，执行侧沙箱白名单兜底）；仅 iter=0 门控用户意图一次。
- 验证：model-loop O1（`rm -rf /` → 0 次模型调用 + 拦截提示落盘）；verify 319/319；audit 0 issues。PLAN T-P1-5 / current-state 事实 7 已按 ADR 口径修正。
- 经验：**读插件挂点要问「这个事件谁发」**——挂在不驱动的事件上等于没挂；同类问题以后排查时先 grep 事件 emit 方。

## v0.7.0（BUG-021：审批死挂点，第三个「服务在链路断」）
- 排查发现审批 UI 全套建好（弹窗/submit IPC/pending map）但：① executeTool 从不发起 approval.request；② 应答方注册错位——挂在 authz 插件的 setUiAnswerer（空接口无消费方），实际发起方是 host-services 的 approval.request（自己的应答器恒 null）。
- 修复：应答方同回调注册到 runtime.host（实际发起方）；shell_command 授权门（白名单准入后 paranoid 拒 / default+trusted 过审批；审批链路不可用 fail-closed——与 ADR-0008 intent 放行边界不同，因命令白名单含万能 shell 兜底不足）；渲染层就绪标记（load-sessions 首调置位，未就绪零等待拒绝）。
- 测试技法：verify-kit stub 的 webSent 捕获 webContents.send → 端到端审批链路（发起→捕获→submit-decision 应答→执行）；**probe 子进程必须等 bootRuntime 就绪**（plugin-runtime handler 注册早于运行时就绪，直接 kick 会竞态得假 fail-closed）。
- verify 320/320；tag v0.7.0 已推送。PRD 三大挂点（intent/trace/approval）至此全部真实生效。
- 经验：「注册错位」是死挂点的变体——审计时对每个 setXxx 注入点要问「消费方是谁、它真的会调用吗」。

## v0.8.0（死挂点清零：promptLib + 语义召回 + file_write 授权门）
- promptLib 第四个死挂点修复：`mergeForAgent('orchdesk-main')` 合并注入 system prompt（含冲突标注）；提示词 agents 留空 = 全 Agent 生效。
- 记忆召回升级：recall 语义 Top-K 优先，**召回为空回落机械取尾**——N2 回归（「我叫什么？」与「用户叫我小星」无词面交集、余弦 0）抓到后加的兜底，用户告知的事实绝不能被召回算法吞掉。
- file_write 接入 approvalGate（与 shell 共用）；probe 加 `__DATA__` 占位（子进程 dataDir 必在白名单内），解决「相对路径落到 stub 白名单外被路径沙箱先拦、测不到授权门」的问题。
- verify 324/324；v0.8.0 tag 已推（网络间歇，直连+代理轮换多轮后成功）。
- **剩余（全部需用户环境）**：真机 GUI 冒烟（E2E 最后做）、真实模型闭环、TRACE 真实上传、代码签名。代码侧死挂点已清零。

## v0.9.0（第五个死挂点：专家团派发）
- 编排此前「看得见目录、发不出任务」：catalog 桥只读、无 compose-team IPC、渲染层无派发入口。
- 修复：`orchdesk:compose-team` 桥（multi composeTeam 真实跑三层）+ preload composeTeam + 插件页团条目「派发任务」按钮（askInput 收任务 → 委派树渲染，state.delegationLast）。
- model-loop Q1 端到端一次通过：Director 层经 agentRunner→callModel→mock 网关真实执行、全节点收敛 done（agentRunner 是 v0.4.0 接入的 SubAgent 运行器，链路早已就绪，只缺入口）。
- verify 325/325；v0.9.0 tag 已推。**PRD 五大挂点全部生效**：intent 网关 / trace 遥测 / 审批弹窗 / memory+promptLib / 专家团派发。
- 代码侧增量项仅剩：compensation web_fetch 门控检查、authz decide（trusted 放宽）、memory 80% 转储挂回合末——都是小项。主体收尾 = 用户环境四件事（真机冒烟/模型/上传/签名）。

## v0.9.1（清单三小项收口：一修两跳过）
- **修**：firePreStep payload 只传单条消息 → memory 80% 阈值检测形同虚设（桥接盲点，非死挂点）；现传完整会话正文（system+历史+当前），intent 取 lastUser 不受影响。
- **跳过 ①**：web_fetch 不接补偿/审批门——只读 GET 无不可逆外发，接入只会徒增弹窗（ponytail：不需要存在）。
- **跳过 ②**：trusted 模式 file_write 免审——被 authz getMode 反查缺陷卡住（find 条件 approvalPolicy==='never'，把 trusted 折叠成 default）；修它要改插件+vendor 重物化，成本/价值不匹配，记为已知债。
- verify 325/325；v0.9.1 tag 已推。**代码侧清单到此收口**，剩用户环境四件事。

## v0.9.1 EXE 本地打包（两个新坑 + 绕法）
- **坑① electron-builder 卡死**：首跑 22 分钟零输出（electron zip 下载被 BUG-W03 网络间歇挂死，tail 管道还吞了进度）。杀掉重跑即过（zip 100%）。**教训：打包日志直接落文件（> build-log.txt），别用 tail 管道——管道缓冲进度，卡死时无法诊断。**
- **坑② 被杀进程遗留锁目录**：重跑报 `Lock file is already being held`（proper-lockfile），锁的真身是残留目录 `release/win-unpacked.tmp`（非 .lock 文件，find 才找到）。沙箱禁删（bulk 守卫）。**最懒绕法：换输出目录 `-c.directories.output=release-v091`**——新目录无锁直接可跑，不跟守卫缠斗。
- 产物：`apps/desktop/release-v091/OrchDesk Setup 0.9.1.exe`（85.2MB NSIS）+ `OrchDesk 0.9.1.exe`（84.8MB portable）+ latest.yml/blockmap，BUILD_EXIT=0，无证书签名跳过（SmartScreen 提示属正常）。源码 package.json 未误伤。
- release-v091/ 与 build-log.txt 已进 .gitignore。

## PRD 完成度重定基线（75% → ≈88%）
- CHECKPOINT 1.1 已按 08-30 复核更新：P0 ~90% / P1 ~85% / P2 ~65%，加权 ≈88%。
- 口径：代码侧死挂点清零后「接线」与「生效」合一；剩余 ~12% 全部是用户环境运行期验证（GUI 冒烟 / 真实模型 / TRACE 上传 / 签名），代码侧写无可写。
- 99-归档 的 08-29 两份盘点文档是历史快照，不改（归档不动规则）。
