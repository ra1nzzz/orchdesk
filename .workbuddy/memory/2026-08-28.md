# 2026-08-28 · OrchDesk 工作日志

## UI/UX 迭代（P6 收口后的体验优化）
- **启动并行化**：`init()` 14 个串行 IPC → `Promise.allSettled` 并行；首屏先 `render()` 再后台加载数据，感知速度大幅提升
- **智能推荐按钮**：`getSmartRecommendations()` 按近 7 天消息主题信号（regex）+ 技能使用频率 TOP1 + 空闲检测，动态生成 4 个快捷按钮；新手首次使用→引导型推荐（创建项目/闲时任务/浏览技能/项目分析）
- **动态时段问候**：`getGreeting()` 扩展为早上好/上午好/中午好/下午好/傍晚好/晚上好/夜深了 +「一起来做点什么呢？」
- **欢迎页布局**：`.home-screen` 由 `flex-start` 改为 `center` 垂直居中；`.composer` 去底部背景（`bg:transparent`/`border:none`/`padding:0`）；`.home-greeting` 20px/600
- **Markdown 渲染升级**：集成 `marked.js`（MIT, UMD, ~45KB 未压缩）替换手写正则渲染器；`renderMD()` 改用 `marked.parse(md, { gfm: true })`；HTML 白名单过滤防注入；Agent 消息自动 MD 渲染（用户消息保持纯文本）
- **列表间隔修复**：`.md-body ul{margin:2px 0}` / `.md-body li{margin:0 0 2px}` 解决渲染间距过宽
- **默认模型下拉**：设置页「模型管理」加默认模型 select（来自所有提供商模型列表），切换后实时重算 `autoSelectModels` + 异步持久化
- **下拉浮窗层级**：`.composer-more-dropdown` z-index 50→200；`.proj-dropdown` z-index 50→200 + `pointer-events:auto/none` 过渡防止关闭态误触
- **项目选择器点击修复**：`.proj-dropdown` 内点击阻止冒泡到 `proj-select-toggle`（prevents dropdown close on item click）

## 模型管理架构修复
- **MODELS 残留引用清理**：L1787 删除提供商时 `MODELS.splice()` → 改为 `dynamicModels` 通过 `bridge.getModelConfig()` 重新刷新；L1823 添加提供商时 `MODELS.push()` → 同上
- **model-confirm 持久化**：确认选择时写入当前会话的 `model` + `models` 字段；清空回退 `autoSelectModels`
- **model-toggle/mg-toggle-all**：确认使用 `getModelPool()`（之前已修复，复查合规）

## 电灯白屏防护
- `BrowserWindow` 加 `backgroundColor: '#1E1E1E'`防止加载期白屏闪烁

## Emoji → SVG 全覆盖
- ctx-empty 占位 `💬` → `ic('clipboard', 28)`；全部图标统一走 `I{}` SVG 路径映射

## 打包 EXE
- **v0.1.0 重建**：`release/OrchDesk Setup 0.1.0.exe`（便携版 + 安装版）；电商 signed with signtool
- **验证**：tsc EXIT=0；E2E Playwright 26/29 PASS（3 项 mock bridge 限制）
- **marked.min.js** 加入 `renderer/vendor/`（UMD，全局暴露 `marked`），`index.html` 通过 `<script>` 标签注入

## 提交并 PUSH
-  docs 知识库 current-state.md 已更新（2026-08-28）
- 待 commit + push 所有改动

## 运行门控（不变）
- 本 agent 环境（WorkBuddy CLI）无法启动 Electron GUI（BUG-W02）；实跑须正常 Windows 桌面双击 exe。
